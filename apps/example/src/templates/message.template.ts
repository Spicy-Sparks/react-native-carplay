import { CarPlay, MessageTemplate } from '@javascriptcommon/react-native-carplay';

export const messageTemplate = new MessageTemplate({
  message: 'Hello, world!',
  debugMessage: 'Lorem ipsum',
  headerAction: { type: 'back' },
});
